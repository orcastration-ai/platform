[package]
name = "platform"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-sdk-dynamodb = "1"
aws-sdk-eventbridge = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
thiserror = "2"
